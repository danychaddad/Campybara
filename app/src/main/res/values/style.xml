<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

    </style>

    <style name="CustomDatePicker" parent="@android:style/Widget.Material.Light.DatePicker">
        <item name="android:datePickerMode">calendar</item>
        <item name="android:headerBackground">@color/primary</item>
        <item name="android:calendarTextColor">@color/black</item>
        <item name="android:dayOfWeekBackground">@color/primary</item>
        <item name="android:dateTextAppearance">@style/CustomDatePickerDay</item>
        <item name="android:weekNumberColor">@color/black</item>
    </style>

    <style name="CustomDatePickerDay">
        <item name="android:textColor">@color/black</item>
        <item name="android:state_selected">@color/white</item>
    </style>
</resources>